
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.8
    hooks:
      - id: ruff
        name: ruff linting
        args: ["--config=pyproject.toml"]
      - id : ruff-format
        name: ruff formatting
        args: ["--config=pyproject.toml"]


